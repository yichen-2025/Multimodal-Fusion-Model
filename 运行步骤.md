# 多模态融合模型运行步骤

## 项目简介

本项目实现了一个基于多模态融合的网络流量分类模型，结合统计特征模态和文本描述模态进行恶意流量检测，并提供消融实验功能验证各模态的有效性。

### 主键体系

项目采用三级主键体系管理数据和模型：

| 主键 | 作用 | 示例 |
|------|------|------|
| `dataset_id` | 标识数据集子集 | `dataset_0.csv`, `dataset_1.csv` |
| `split_id` | 标识同一数据集的不同划分 | `split_data/dataset_0/split_0/` |
| `model_id` | 标识不同的训练结果 | `saved_models/model_0/` |

### 消融实验变体

| 变体 | 数值模态 | 文本模态 | LLM | 描述 |
|------|---------|---------|-----|------|
| A0 | ✓ | ✓ | ✓ | 全模型（数值+文本+LLM） |
| A1 | ✓ | ✗ | ✓ | 仅数值+LLM（无文本） |
| A2 | ✗ | ✓ | ✓ | 仅文本+LLM（无数值） |
| A3 | ✓ | ✓ | ✗ | 无LLM（纯MLP分类） |

---

## 运行方式

项目通过 `main.py` 文件统一管理所有运行步骤，使用方法如下：

1. 打开 `main.py` 文件
2. 取消注释对应步骤的代码块（一次只取消一个步骤）
3. 运行命令：`python main.py`
4. 步骤完成后，注释该步骤代码，继续下一步

---

## 步骤详解

### 步骤0：安装依赖

```bash
pip install torch transformers datasets scikit-learn pandas numpy pytest matplotlib
```

> 默认已完成，若缺少依赖可执行上述命令。

### 步骤0：下载预训练模型

```bash
python tools/download_bert.py
python tools/download_qwen.py
```

- **涉及文件**：`tools/download_bert.py`, `tools/download_qwen.py`
- **模型路径**：`./models/bert/`, `./models/qwen2.5-1.5b/`

---

### 步骤1：数据清洗与预处理

**操作**：取消 `main.py` 中步骤1的注释

```python
# ============================================
# 步骤1：数据清洗
# ============================================
from scripts.data_cleaning import main as run_data_cleaning
run_data_cleaning()
```

**运行**：`python main.py`

**输入**：`data_processing/` 目录下的CSV文件

**输出**：`processed_dataset/processed_dataset.csv`

**处理内容**：缺失值处理、异常值检测、标签编码（BENIGN→0, DDoS→1）

---

### 步骤2：提取数据集子集

**操作**：取消 `main.py` 中步骤2的注释

```python
# ============================================
# 步骤2：提取数据集子集
# ============================================
success, dataset_id = extract_subset(
    num_samples=5000,
    # dataset_id=0,  # 可选：指定数据集ID
    random_state=42
)
if success:
    print(f"成功提取子集，数据集ID: {dataset_id}")
else:
    print("提取子集失败")
```

**运行**：`python main.py`

**参数说明**：
- `num_samples`: 提取样本数量（默认5000）
- `dataset_id`: 数据集ID（默认自动递增）
- `random_state`: 随机种子（默认42）

**输出**：
- `processed_dataset/dataset_X.csv`（子集数据）
- `processed_dataset/subset_X_scaled_features.npy`（标准化特征）
- `processed_dataset/subset_X_labels.npy`（标签）
- `processed_dataset/subset_X_scaler.npy`（标准化器参数）

**日志记录**：自动生成 `logs/subset/log_X.json`

---

### 步骤3：模态分离与数据集划分

**操作**：取消 `main.py` 中步骤3的注释，确保 `dataset_id` 与步骤2一致

```python
# ============================================
# 步骤3：模态分离与数据集划分
# ============================================
dataset_id = 0  # 与步骤2的dataset_id一致
split_modality(
    dataset_id=dataset_id,
    # split_id=0,  # 可选：指定划分ID
    test_size=0.2,
    random_state=42
)
print(f"成功完成模态分离，数据集ID: {dataset_id}")
```

**运行**：`python main.py`

**参数说明**：
- `dataset_id`: 数据集ID（默认0）
- `split_id`: 划分ID（默认自动递增）
- `test_size`: 测试集比例（默认0.2）
- `val_size`: 验证集比例（默认0.1）
- `random_state`: 随机种子（默认42）

**输出**：`split_data/dataset_X/split_Y/`
- `train.npz`（训练集：统计特征 + BERT嵌入 + 标签）
- `val.npz`（验证集：统计特征 + BERT嵌入 + 标签）
- `test.npz`（测试集：统计特征 + BERT嵌入 + 标签）
- `train_data.csv` / `val_data.csv` / `test_data.csv`（原始数据备份）
- `train_scaler.npy`（训练集标准化器参数）

**日志记录**：自动生成 `logs/split/log_X.json`

---

### 步骤4：消融实验

**操作**：取消 `main.py` 中步骤4的注释，确保 `dataset_id` 和 `split_id` 与步骤3一致

```python
# ============================================
# 步骤4：消融实验
# ============================================
run_ablation(
    variants=["A0","A1","A2","A3"],
    dataset_id=0,
    split_id=0,
    repeat=1,
    seed=42
)
```

**运行**：`python main.py`

**参数说明**：
- `variants`: 消融变体列表（默认 `["A0","A1","A2","A3"]`）
- `dataset_id`: 数据集ID（默认0）
- `split_id`: 划分ID（默认0）
- `repeat`: 每个变体的重复次数（默认1）
- `seed`: 随机种子（默认42）

**输出**：`ablation_results/`
- `ablation_results_时间戳.csv`（完整实验结果表）

**说明**：
- 每个变体自动完成训练和测试，生成独立的模型和测试报告
- 无LLM变体（A3）可在CPU上运行，其余变体需要GPU
- 若 `repeat > 1`，结果将包含多次运行的均值和标准差

**日志记录**：每个变体自动生成 `logs/training/log_X.json` 和 `test_reports/report_X.json`

---

### 步骤5：绘制消融实验对比图

**操作**：取消 `main.py` 中步骤5的注释

```python
# ============================================
# 步骤5：绘制消融实验结果
# ============================================
results_df = pd.read_csv("ablation_results/ablation_results_时间戳.csv")
plot_f1_comparison(results_df)
```

**运行**：`python main.py`

**参数说明**：
- `results_df`: 步骤4输出的CSV文件路径

**输出**：`ablation_results/ablation_comparison_时间戳.png`

**效果**：生成包含 Accuracy、Precision、Recall、F1 四项指标的分组柱状图

---

## 完整流程示例

打开 `main.py`，按顺序取消注释执行：

```python
# 第1次运行：数据清洗
from scripts.data_cleaning import main as run_data_cleaning
run_data_cleaning()

# 第2次运行：提取子集
success, dataset_id = extract_subset(num_samples=5000, random_state=42)

# 第3次运行：模态分离
dataset_id = 0
split_modality(dataset_id=dataset_id, test_size=0.2, random_state=42)

# 第4次运行：消融实验
run_ablation(variants=["A0","A1","A2","A3"], dataset_id=0, split_id=0, repeat=1, seed=42)

# 第5次运行：绘制对比图
results_df = pd.read_csv("ablation_results/ablation_results_时间戳.csv")
plot_f1_comparison(results_df)
```

---

## 注意事项

### 硬件要求

- **GPU**：推荐 NVIDIA GPU（Ampere架构及以上），支持bfloat16混合精度训练
- **显存**：至少16GB（训练Qwen2.5-1.5B模型）
- **内存**：至少16GB（处理大数据集）

### 数据格式

- 原始数据集：CSV格式，包含`Label`列（值为`BENIGN`或`DDoS`）
- 支持的特征列：`Destination Port`, `Bwd Packet Length Mean`, `Avg Bwd Segment Size`, `Bwd Packet Length Max`, `Bwd Packet Length Std`, `URG Flag Count`, `Packet Length Mean`, `Average Packet Size`, `Packet Length Std`

### 日志系统

所有操作都会自动生成日志文件：

| 日志类型 | 保存位置 | 内容 |
|---------|---------|------|
| 子集提取 | `logs/subset/log_X.json` | 时间、数据集ID、样本数、正负样本数 |
| 数据划分 | `logs/split/log_X.json` | 时间、数据集ID、划分ID、训练/验证/测试样本数 |
| 模型训练 | `logs/training/log_X.json` | 时间、模型ID、数据集ID、变体、训练参数、耗时 |
| 模型测试 | `test_reports/report_X.json` | 时间、模型ID、变体、评估指标、混淆矩阵 |

### 测试脚本

运行项目测试套件验证所有功能：

```bash
python -m pytest tests/test_project.py -v
```

---

## 文件结构

```
Multimodal-Fusion-Model/
├── main.py                        # 主入口文件（推荐运行方式）
├── README.md                      # 项目说明文档
├── 运行步骤.md                    # 详细运行步骤文档
├── 原理图.png                      # 项目原理图
├── 数据集处理流程图.png             # 数据处理流程图
├── .gitignore                     # Git忽略规则
│
├── scripts/                       # 运行脚本
│   ├── data_cleaning.py           # 数据清洗
│   ├── extract_subset.py          # 子集提取
│   ├── split_modality.py          # 模态分离
│   ├── train.py                   # 模型训练（支持消融变体）
│   ├── test_model.py              # 模型测试
│   └── run_ablation.py            # 消融实验运行器
│
├── tools/                         # 工具脚本
│   ├── download_bert.py           # BERT下载
│   └── download_qwen.py           # Qwen下载
│
├── src/                           # 核心源码
│   ├── data/
│   │   └── data_loader.py         # 数据加载模块
│   └── model_architectures/       # 模型架构
│       ├── bert_encoder.py        # BERT文本编码器
│       ├── numeric_encoder.py     # 数值特征编码器
│       ├── fusion_projection.py   # 特征融合投影层
│       └── multi_modal_model.py   # 多模态融合模型
│
├── utils/                         # 工具模块
│   └── log_utils.py               # 日志记录工具
│
├── tests/                         # 测试套件
│   └── test_project.py            # 项目测试
│
├── docs/                          # 文档
│   └── theory.md                  # 项目原理说明
│
├── data_processing/               # 原始数据
│   └── *.csv
│
├── processed_dataset/             # 处理后数据（自动生成）
│   ├── processed_dataset.csv
│   └── dataset_X.csv
│
├── split_data/                    # 划分后数据（自动生成）
│   └── dataset_X/split_Y/
│       ├── train.npz
│       ├── val.npz
│       ├── test.npz
│       └── ...
│
├── saved_models/                  # 训练模型（自动生成）
│   └── model_X/
│       ├── pytorch_model.bin
│       ├── config.txt
│       └── loss_logs/
│
├── ablation_results/              # 消融实验结果（自动生成）
│   ├── ablation_results_*.csv
│   └── ablation_comparison_*.png
│
├── models/                        # 预训练模型（需下载）
│   ├── bert/
│   └── qwen2.5-1.5b/
│
├── logs/                          # 操作日志（自动生成）
│   ├── subset/
│   ├── split/
│   └── training/
│
└── test_reports/                  # 测试报告（自动生成）
    ├── reports_index.csv
    └── report_X.json
```